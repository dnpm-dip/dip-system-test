events {
    worker_connections 1024;
}

http {
    # CCDN sends status=Unsubmitted (capital); DIP backend expects lowercase.
    map $args $normalized_args {
        "status=Unsubmitted"  "status=unsubmitted";
        default               $args;
    }

    include /etc/nginx/sites-enabled/*.conf;
}
